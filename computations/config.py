# sizes of generated instances for SMTI
sizes_smti = [i for i in range(3, 31)]

# how many samples are created per size cat
samples_per_size_smti = 50

# sizes of matchings created for SMP
sizes_smp = [i for i in range(3, 31)]

# sizes of that can be solved by the QA (2020)
sizes_smp_qa = [i for i in range(3, 8)]

# how many samples are created for SMP per instance
samples_per_size_smp = 20

# sizes that can be solved by BACKTRACKING in reasonable time
smp_solve_max = 19
